GPT-2 Fine-Tuning Pipeline
This project demonstrates how to fine-tune the GPT-2 language model using Hugging Face Transformers and Datasets on a custom text file.

🚀 Features
Loads custom dataset (custom_data.txt)
Tokenizes text using GPT-2 tokenizer
Fine-tunes pretrained GPT-2 model
Generates text after training 

🛠️ Requirements

pip install transformers datasets torch

GPT-2-Fine-Tuning/
│── gpt2.py
│── README.md
│── requirements.txt
